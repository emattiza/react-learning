
export const SignInButton = () => {
  const handleClick = () => {
    // const auth = useAuth();
  };

  return (
    <button
      onClick={handleClick}
      type="button"
      className="btn btn-primary normal-case min-w-60"
    >
      Sign In With Google
    </button>
  );
};
